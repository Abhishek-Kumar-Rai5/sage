"""IR Builder package.

Consumes the complete canonical Document model during extraction and
produces an IRDataset. This package contains only the builder-facing
interface for Week 2; the extraction logic that populates entity fields
from Document content is out of scope for this week and is implemented in
a later phase.
"""